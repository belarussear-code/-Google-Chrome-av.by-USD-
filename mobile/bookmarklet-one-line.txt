javascript:(async function avbyUsdBookmarklet() { const INJ = "avbyusd-bookmarklet"; const SEL = ".listing-item__price-primary, [class*='listing-item__price-primary'], .listing-index__price, [class*='listing-index__price'], .listing-top__price-primary, [class*='listing-top__price-primary'], .card__price-button, [class*='card__price-button']"; const style = "font-size:13px;font-weight:600;color:#1565c0;margin-top:4px;line-height:1.35;"; const hintStyle = "font-size:11px;font-weight:400;color:#607d8b;margin-top:2px;display:block;"; function normalizeWs(text) { return (text || "") .replace(/[\u00a0\u2009\u202f\u2007\u00ad\ufeff]/g, " ") .replace(/\s+/g, " ") .trim(); } function parseBynPrimary(text) { const c = normalizeWs(text); if (!c || /договорн|по запросу|цена не указ/i.test(c)) return 0; if (!/(?:\u0440|р|p)\./i.test(c)) return 0; let x = c .replace(/\s*(?:\u0440|р|p)\.\s*$/i, "") .replace(/\s*\u0440\u0443\u0431\.?\s*$/i, "") .trim(); x = x.replace(/\s/g, "").replace(",", "."); const n = parseFloat(x); return Number.isFinite(n) && n > 50 ? n : 0; } async function rateBynPerUsd() { try { const html = await ( await fetch("https://myfin.by/currency/usd", { credentials: "omit" }) ).text(); const m = html.match( /data-popover-selector="nbrbUSD"[\s\S]{0,5000}?<span class="accent">\s*([\d.,]+)\s*</i ); if (m) { const v = parseFloat(String(m[1]).replace(",", ".")); if (Number.isFinite(v) && v > 0) return { v, src: "myfin.by (НБРБ)" }; } } catch (_) {} const j = await ( await fetch( "https://api.nbrb.by/exrates/rates/USD?parammode=2&periodicity=0", { credentials: "omit" } ) ).json(); const scale = Number(j.Cur_Scale) || 1; const r = Number(j.Cur_OfficialRate); return { v: r / scale, src: "api.nbrb.by" }; } document.querySelectorAll("." + INJ).forEach((n) => n.remove()); const { v: bynPerUsd, src } = await rateBynPerUsd(); const priceEls = [...document.querySelectorAll(SEL)]; let n = 0; for (const el of priceEls) { const byn = parseBynPrimary(el.textContent || ""); if (!byn) continue; const box = document.createElement("div"); box.className = INJ; box.setAttribute("style", style); box.textContent = "≈ " + Math.round(byn / bynPerUsd).toLocaleString("ru-BY") + " $"; const hint = document.createElement("span"); hint.setAttribute("style", hintStyle); hint.textContent = "курс " + bynPerUsd.toLocaleString("ru-BY", { minimumFractionDigits: 2, maximumFractionDigits: 4, }) + " BYN за 1 USD · " + src + " · закладка"; box.appendChild(hint); el.insertAdjacentElement("afterend", box); n++; } if (typeof alert !== "undefined") { alert( n ? "Готово: " + n + " цен(ы) (listing / card)." : "Не найдено блоков цены (listing / index / card) с «… р.»." ); } })();